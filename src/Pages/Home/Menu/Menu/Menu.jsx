import React from 'react';
import { Helmet} from 'react-helmet-async';

const Menu = () => {
    return (
        <div>
            <Helmet>
                <title>Mern Express || Menu</title>
            </Helmet>
            <h2>this is menu</h2>
        </div>
    );
};

export default Menu;